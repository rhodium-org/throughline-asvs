uid: SR-0117
type: system_requirement
status: approved
title: The application enforces access control rules on a trusted service layer, especially
  if client-side access control is present and could be bypassed
text: Verify that the application enforces access control rules on a trusted service
  layer, especially if client-side access control is present and could be bypassed.
links:
- target: UR-0004
  type: implements
attrs:
  source_ref: V4.1.1
  level: 1
