uid: SR-0254
type: system_requirement
status: approved
title: GraphQL or other data layer authorization logic should be implemented at the
  business logic layer instead of the GraphQL layer
text: Verify that GraphQL or other data layer authorization logic should be implemented
  at the business logic layer instead of the GraphQL layer.
links:
- target: UR-0013
  type: implements
attrs:
  source_ref: V13.4.2
  level: 2
