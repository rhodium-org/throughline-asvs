uid: SR-0194
type: system_requirement
status: approved
title: The OAuth client only requests the required scopes (or other authorization
  parameters) in requests to the authorization server
text: Verify that the OAuth client only requests the required scopes (or other authorization
  parameters) in requests to the authorization server.
links:
- target: UR-0010
  type: implements
attrs:
  source_ref: V10.2.3
  level: 3
