uid: SR-0311
type: system_requirement
status: approved
title: JavaScript code is written in a way that prevents prototype pollution, for
  example, by using Set() or Map() instead of object literals
text: Verify that JavaScript code is written in a way that prevents prototype pollution,
  for example, by using Set() or Map() instead of object literals.
links:
- target: UR-0015
  type: implements
attrs:
  source_ref: V15.3.6
  level: 2
