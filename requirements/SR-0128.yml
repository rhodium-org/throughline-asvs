uid: SR-0128
type: system_requirement
status: approved
title: All input (HTML form fields, REST requests, URL parameters, HTTP headers, cookies,
  batch files, RSS feeds, etc) is validated using positive validation (allow lists)
text: Verify that all input (HTML form fields, REST requests, URL parameters, HTTP
  headers, cookies, batch files, RSS feeds, etc) is validated using positive validation
  (allow lists).
links:
- target: UR-0005
  type: implements
attrs:
  source_ref: V5.1.3
  level: 1
