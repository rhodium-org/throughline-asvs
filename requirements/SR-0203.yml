uid: SR-0203
type: system_requirement
status: approved
title: Only the latest recommended versions of the TLS protocol are enabled, such
  as TLS 1.2 and TLS 1.3
text: Verify that only the latest recommended versions of the TLS protocol are enabled,
  such as TLS 1.2 and TLS 1.3. The latest version of the TLS protocol should be the
  preferred option.
links:
- target: UR-0009
  type: implements
attrs:
  source_ref: V9.1.3
  level: 1
