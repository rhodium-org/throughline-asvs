uid: SR-0064
type: system_requirement
status: approved
title: If PBKDF2 is used, the iteration count SHOULD be as large as verification server
  performance will allow, typically at least 100,000 iterations
text: Verify that if PBKDF2 is used, the iteration count SHOULD be as large as verification
  server performance will allow, typically at least 100,000 iterations.
links:
- target: UR-0002
  type: implements
attrs:
  source_ref: V2.4.3
  level: 2
