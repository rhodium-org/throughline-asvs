uid: SR-0191
type: system_requirement
status: approved
title: Data stored in browser storage (such as localStorage, sessionStorage, IndexedDB,
  or cookies) does not contain sensitive data
text: Verify that data stored in browser storage (such as localStorage, sessionStorage,
  IndexedDB, or cookies) does not contain sensitive data.
links:
- target: UR-0008
  type: implements
attrs:
  source_ref: V8.2.2
  level: 1
