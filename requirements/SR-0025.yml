uid: SR-0025
type: system_requirement
status: approved
title: The application uses memory-safe string, safer memory copy and pointer arithmetic
  to detect or prevent stack, buffer, or heap overflows
text: Verify that the application uses memory-safe string, safer memory copy and pointer
  arithmetic to detect or prevent stack, buffer, or heap overflows.
links:
- target: UR-0001
  type: implements
attrs:
  source_ref: V1.4.1
  level: 2
