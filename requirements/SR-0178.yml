uid: SR-0178
type: system_requirement
status: approved
title: The application enforces authorization rules at a trusted service layer and
  doesn't rely on controls that an untrusted consumer could manipulate, such as client-side
  JavaScript
text: Verify that the application enforces authorization rules at a trusted service
  layer and doesn't rely on controls that an untrusted consumer could manipulate,
  such as client-side JavaScript.
links:
- target: UR-0008
  type: implements
attrs:
  source_ref: V8.3.1
  level: 1
