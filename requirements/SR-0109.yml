uid: SR-0109
type: system_requirement
status: approved
title: Cookie-based session tokens use the "__Host-" prefix so cookies are only sent
  to the host that initially set the cookie
text: Verify that cookie-based session tokens use the "__Host-" prefix so cookies
  are only sent to the host that initially set the cookie.
links:
- target: UR-0003
  type: implements
attrs:
  source_ref: V3.4.4
  level: 1
