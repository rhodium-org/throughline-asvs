uid: SR-0130
type: system_requirement
status: approved
title: URL redirects and forwards only allow destinations which appear on an allow
  list, or show a warning when redirecting to potentially untrusted content
text: Verify that URL redirects and forwards only allow destinations which appear
  on an allow list, or show a warning when redirecting to potentially untrusted content.
links:
- target: UR-0005
  type: implements
attrs:
  source_ref: V5.1.5
  level: 1
